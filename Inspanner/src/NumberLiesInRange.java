//using logical operator to check is the number lies in the range 97 and 122
public class NumberLiesInRange {
    public static void main(String[] args) {
        int number = 9;
        System.out.println((number >= 97 && number <= 122) ? "Lies in the range" : "Doesn't lie in the range");
    }
}
